/*
              const hello = async () => {
                alert('hi! it\'s working');
                const x = await fetch('');
              }

              hello();
              */

// [ WebPack 문법 ] 브라우저단에서 실행될 코드를 보관할 폴더명을 client 로 작명함

alert("hi");
